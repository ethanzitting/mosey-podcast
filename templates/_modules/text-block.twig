{#
/**
 * Available variables:
 * - image: the background image for the slider
 * - slides: an array of slide objects which have quote and byline attributes
 */
#}

<section class="text-block container">
    Text Block Module, probably further information about the podcast.
</section>